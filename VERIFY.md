# 검증 프로세스

네 개 층으로 나눈다. 아래 세 층은 자동, 마지막 층은 사람이 한다.

```bash
python3 scripts/verify.py          # 1~3층 한 번에
python3 scripts/verify.py --quiet  # 실패한 층만
```

| 층 | 도구 | 대상 | 실패하면 |
| --- | --- | --- | --- |
| 1 구조 | `scripts/audit.py` | `data/series.json` · `data/episodes.json` | 데이터가 틀렸다 |
| 2 서사 | `scripts/canon_check.py` | `data/canon/*.json` + `drafts/` | 이야기의 논리가 틀렸다 |
| 3 문체 | `scripts/style_check.py` | `drafts/` | 문장이 관행에서 벗어났다 |
| 4 사람 | `cutlines.html` · 외부 파일럿 | 읽는 경험 | 재미가 없다 |

세 층은 순서가 있다. **구조가 틀린 채로 본문을 쓰면 본문을 다시 써야 한다.**
컷 라인이 다른 회차를 가리키고 있던 22편이 그 사례다.

## 1층 구조

읽지 않고 데이터에서 잡히는 결함.

- **정합성** — 회차 수, 번호 연속, 무료 컷 위치, `isFree` 플래그
- **페이스** — 무료분이 전부 발단, 위기 단계 없음, 발단 과다, 절정 지연, 같은 막 8화 연속, 컷이 발단/결말에 걸림
- **페이월** — `cutline-drift`(컷 라인이 컷 회차를 가리키는가), `secret-early`, `declarative-cutline`, 무료 비율
- **카탈로그** — 슬롯 3개 이상 겹치는 중복, 제목·로그라인 유사도, 장르 쏠림

기계가 판정할 수 없는 findings 는 사람이 읽고 `data/reviewed.json` 에 이유와 함께 기록한다.

## 2층 서사

`data/canon/<시리즈>.json` 에 일곱 가지를 적고 초고와 대조한다.

| 단계 | 적는 것 | 기계가 잡는 것 |
| --- | --- | --- |
| 1 고정 설정표 | 혼인·출산·직업·재산·거주·과거 사건, 그리고 **금지표현** | 금지표현이 초고에 남아 있는지 |
| 2 인물별 정보보유표 | 회차마다 누가 무엇을 알고 모르는지 | 회차 누락, 알던 것을 다시 모르게 되는 역행 |
| 3 원인–행동–결과 | 주요 행동마다 "왜 지금 이 행동을 하는가" | 행동 없는 회차, 빈 why/result |
| 4 반전 역검증 | 복선을 심은 화 / 회수하는 화 / 재독 시 해석 | 순서 역전, 아직 안 쓴 화에 심었다는 기록 |
| 5 현실성 검증 | 제도·절차와 다른 부분, 상태와 근거 | 상태값 오류, 근거 누락 |
| 6 회차 삭제 테스트 | 회차마다 새 사실·선택·관계 변화 | 셋 다 없는 회차 = 빼도 되는 회차 |
| 7 결제 반대 심사 | 무료 마지막 화에서 멈출 이유와 그 반론 | 공란, 잘못된 판정값 |

`draftedThrough` 까지만 검사한다. 아직 안 쓴 회차는 대상이 아니다.

4번 재독 해석은 이렇게 적는다 — 두 번째로 봤을 때 **"그래서 저랬구나"** 가 되어야 하고
**"작가가 일부러 숨겼네"** 가 되면 안 된다.

5번은 근거를 남긴다. 상태는 `확인` / `가정` / `일반 사양` / `정정 완료` 넷 중 하나다.
`가정`으로 남긴 항목은 실제 제도와 다를 수 있다는 뜻이고, 그대로 두려면 그 이유가 적혀 있어야 한다.

## 3층 문체

기준과 근거는 `STYLE.md`. 공백 포함 5,000자 ±10%, 문장 평균 25자 이하,
대사 25% 이상, 지문 연속 400자 이하, 문단 평균 2줄 이하.

## 4층 사람

자동화하지 않는다.

1. **블라인드 컷 판정** — `cutlines.html`. 제목과 장르를 가린 채 컷 라인만 읽고
   결제할지 판정한다. 시리즈를 알면 이미 궁금해진 상태라 판정이 오염된다.
2. **1화 이탈 테스트** — 1화만 읽히고 "2화 볼래?"를 묻는다. 5명 중 3명 미만이면 버린다.
3. **런칭 후 지표** — 1화 이탈률, 무료 마지막 화 완독률, 완독→결제 전환율.

## 순서

```
회차 시놉시스 → 1층 통과 → canon 작성 → 본문 집필 → 2·3층 통과 → 4층
```

canon 을 본문보다 먼저 쓰는 이유는, 정보보유표가 없으면 인물이 알 수 없는 것을
말하는 대사를 쓰게 되고 그건 다 쓰고 나서는 못 고치기 때문이다.
